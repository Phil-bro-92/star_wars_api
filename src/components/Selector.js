import Option from "./Option";

const Selector = () => {
  return (
    <select defaultvalue="">
      <option value="">Select a Category</option>
      <Option/>
    </select>
  );
};

export default Selector;
